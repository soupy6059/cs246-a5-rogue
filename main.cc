import <memory>;
import <iostream>;
import <vector>;

using namespace std;

int main() {
    try {
    }
    catch(const exception &ex) {
        cerr << ex.what() << endl;
        return -1;
    }
}
